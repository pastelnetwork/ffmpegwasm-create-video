{
  "name": "react-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@ffmpeg/core": "0.10.0",
    "@ffmpeg/ffmpeg": "0.10.1",
    "cross-env": "7.0.3",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "start": "cross-env NODE_ENV=development PORT=3009 react-scripts start",
    "build": "cross-env NODE_ENV=production react-scripts build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "css-loader": "^5.2.6"
  }
}
